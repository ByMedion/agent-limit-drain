[project]
name = "agent-limit-drain"
version = "0.1.0"
description = "Community tracking of AI agent usage limits versus actual token usage"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "plotly>=6.0.0",
    "kaleido>=1.3.0",
]
