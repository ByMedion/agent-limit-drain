# TODO

- [ ] Add a Drain Factor calculator to the website so visitors can enter their token usage and limit consumption and calculate their own factor without contributing data.

- [ ] Add variability visualization for aggregated observations once enough community data is available. Keep the pooled Drain Factor as the primary line, and show the spread of individual observations (for example min/max or error bars). When sample sizes become large enough, consider median + quartiles / box plots instead of candlestick-style OHLC semantics.
